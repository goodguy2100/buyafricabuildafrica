name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate route tree
        run: bun x @tanstack/router-cli generate

      - name: Build
        run: bun run build

      - name: Create index.html with relative paths
        run: |
          # Find the actual JS and CSS files
          JS_FILE=$(ls dist/client/assets/index-*.js 2>/dev/null | head -1)
          CSS_FILE=$(ls dist/client/assets/styles-*.css 2>/dev/null | head -1)
          JS_NAME=$(basename "$JS_FILE")
          CSS_NAME=$(basename "$CSS_FILE")
          echo "JS: $JS_NAME"
          echo "CSS: $CSS_NAME"
          
          # Use relative paths (no leading /) since we're in a subdirectory
          cat > dist/client/index.html << 'HTMLEOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="utf-8" />
            <meta name="viewport" content="width=device-width, initial-scale=1.0" />
            <title>Buy Africa Build Africa (BABA)</title>
            <link rel="preconnect" href="https://fonts.googleapis.com" />
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
            <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
            <link rel="stylesheet" href="assets/CSS_PLACEHOLDER" />
          </head>
          <body>
            <div id="root"></div>
            <script type="module" crossorigin src="assets/JS_PLACEHOLDER"></script>
          </body>
          </html>
          HTMLEOF
          # Replace placeholders
          sed -i "s|CSS_PLACEHOLDER|$CSS_NAME|g" dist/client/index.html
          sed -i "s|JS_PLACEHOLDER|$JS_NAME|g" dist/client/index.html
          echo "--- index.html ---"
          cat dist/client/index.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/client

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
