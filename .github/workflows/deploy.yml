name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate route tree
        run: bun x @tanstack/router-cli generate

      - name: Build
        run: bun run build

      - name: Create index.html from build assets
        run: |
          # Find the actual JS and CSS files in the build output
          JS_FILE=$(ls dist/client/assets/index-*.js 2>/dev/null | head -1)
          CSS_FILE=$(ls dist/client/assets/styles-*.css 2>/dev/null | head -1)
          JS_NAME=$(basename "$JS_FILE")
          CSS_NAME=$(basename "$CSS_FILE")
          echo "JS: $JS_NAME"
          echo "CSS: $CSS_NAME"
          
          # Create index.html that loads all JS bundles
          cat > dist/client/index.html << HTMLEOF
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="utf-8" />
            <meta name="viewport" content="width=device-width, initial-scale=1.0" />
            <title>Buy Africa Build Africa (BABA)</title>
            <link rel="preconnect" href="https://fonts.googleapis.com" />
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
            <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
            <link rel="stylesheet" href="/assets/$CSS_NAME" />
          </head>
          <body>
            <div id="root"></div>
            <script type="module" crossorigin src="/assets/$JS_NAME"></script>
          </body>
          </html>
          HTMLEOF
          echo "--- index.html ---"
          cat dist/client/index.html
          
          # Also create _redirects for SPA
          echo "/*    /index.html   200" > dist/client/_redirects

      - name: Verify build output
        run: ls -la dist/client/ && ls -la dist/client/assets/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/client

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
